[package]
name = "example"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate_type = ["cdylib"]

[dependencies]
android-activity = { version = "0.6", features = ["native-activity"] }
android-support = { path = ".." }
